<section
  class="relative h-[calc(100%+4rem)] overflow-hidden min-h-screen rounded-t-[40px] home-hero"
>
  <!-- Fondo -->
  <div
    class="absolute inset-0 z-0 bg-cover bg-center"
    style={`background-image: url('/images/general/bg.webp');`}
    aria-hidden="true"
  >
    <div class="absolute inset-0 bg-black/60 z-10"></div>
    <div class="absolute inset-x-0 bottom-0 h-10 bg-bg z-20 rounded-t-[40px]">
    </div>
  </div>
  <div
    class="container mx-auto px-6 pt-32 pb-48 relative z-10 home-hero__inner"
    x-data="{ fadeIn: false }"
    x-init="setTimeout(() => fadeIn = true, 500)"
  >
    <div
      class="w-full transition-all duration-1000 justify-center text-center"
      :class="fadeIn ? 'opacity-100 transform translate-y-0' : 'opacity-0 transform translate-y-10'"
    >
      <h1
        class="text-2xl md:text-4xl font-medium sigmar-regular text-white leading-tight mb-6 home-hero__title"
      >
        BAR RESTAURANTE<br /><span
          class="text-8xl md:text-10xl font-extrabold tracking-widest text-white sigmar-regular block -mt-2 home-hero__brand"
          >ARCADIA</span
        >
      </h1>
    </div>
  </div>
</section>

<style>
  @media (max-width: 639px) {
    .home-hero {
      min-height: 100svh;
      border-top-left-radius: 24px;
      border-top-right-radius: 24px;
    }

    .home-hero [aria-hidden="true"] > .rounded-t-\[40px\] {
      border-top-left-radius: 24px;
      border-top-right-radius: 24px;
      height: 1.75rem;
    }

    .home-hero__inner {
      padding-left: 1rem;
      padding-right: 1rem;
      padding-top: 6.5rem;
      padding-bottom: 7rem;
    }

    .home-hero__title {
      margin-bottom: 1rem;
      line-height: 1.15;
    }

    .home-hero__brand {
      font-size: clamp(3.5rem, 18vw, 5.75rem);
      line-height: 0.95;
      letter-spacing: 0.08em;
      margin-top: 0.25rem;
    }
  }
</style>